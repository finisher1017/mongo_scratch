<h1>EDITING TONER</h1>
 
<%= form_for :toner, url: toner_path(@toner), method: :patch do |toner| %>
 
  <% if @toner.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@toner.errors.count, "error") %> prohibited
        this toner from being saved:
      </h2>
      <ul>
        <% @toner.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= toner.label :manufacturer %>
    <%= toner.text_field :manufacturer %>
  </p>
 
  <p>
    <%= toner.label :equipmentModel %>
    <%= toner.text_field :equipmentModel %>
  </p>

  <p>
    <%= toner.label :color %>
    <%= toner.text_field :color %>
  </p>

  <p>
    <%= toner.label :replacementId %>
    <%= toner.text_field :replacementId %>
  </p>

  <p>
    <%= toner.label :quantity %>
    <%= toner.text_field :quantity %>
  </p>

 
  <p>
    <%= toner.submit %>
  </p>
 
<% end %>
 

<%= link_to 'Back', toners_path %>